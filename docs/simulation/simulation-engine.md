# Simulation Engine

OpenForge simulation uses deterministic ticks and scheduled signals so automation behavior can be replayed and inspected.

## Core Concepts

- Simulation clock controls tick advancement.
- Scenarios define initial signals and scheduled signals.
- Runtime traces record observable behavior.
- Replay uses recorded events to inspect workflow execution after the run.
